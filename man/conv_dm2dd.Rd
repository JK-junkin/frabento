% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.r
\name{conv_dm2dd}
\alias{conv_dm2dd}
\title{Convert degree-minute coordinates to decimal degrees}
\usage{
conv_dm2dd(dm60, original_value = FALSE, num_as_dm = FALSE)
}
\arguments{
\item{dm60}{A character or numeric vector containing coordinates in
degree-minute format.}

\item{original_value}{Logical. If \code{TRUE}, the original input values are
retained as names of the returned vector. Default: \code{FALSE}.}

\item{num_as_dm}{Logical. If \code{TRUE}, numeric inputs are interpreted as
degree-minute values rather than decimal degrees. Default: \code{FALSE}.}
}
\value{
A numeric vector of decimal degree values.
If \code{original_value = TRUE}, the original input values are attached as
names to the output vector.
}
\description{
Convert latitude and longitude values from degree-minute (DM) format
to decimal degrees (DD). Both character and numeric inputs are supported.

Character inputs may contain separators such as periods, spaces, hyphens,
degree symbols, minute symbols, and second symbols.

Numeric inputs are treated as decimal degrees by default. Set
\code{num_as_dm = TRUE} to interpret numeric values as degree-minute
coordinates.
}
\details{
Character inputs are automatically parsed from common degree-minute
notations. One or more consecutive non-numeric separator characters are
collapsed into a single delimiter.

Supported examples include:
\itemize{
\item \code{"35.30"}
\item \code{"35. 3"}
\item \code{"35°30′"}
\item \code{"30°30′750″"} (degree-minute-second style)
\item \code{"30°30.750′"} (fractional minutes)
\item \code{"-135°30′"}
\item \code{"35...30"}
}

Minute values greater than or equal to 60 are considered invalid and
converted to \code{NA} with a warning.
}
\examples{
dmx <- c(
  "35",
  "35.30",
  "35.3",
  "35.03",
  "35. 3",
  "35°30′",
  "30°30′750″",
  "30°30.750′",
  "-135°30′",
  "35...---30",
  "35...   3"
)
conv_dm2dd(dmx)
conv_dm2dd(dmx, original_value = TRUE)

dmnum <- c(34.30, 34.3, 34.03, 34, -120.5)
conv_dm2dd(dmnum)
conv_dm2dd(dmnum, num_as_dm = TRUE)

}
