% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wmap.r
\name{wmap_sf}
\alias{wmap_sf}
\title{create an sf worldmap layer or gg}
\usage{
wmap_sf(
  sfmap = NULL,
  sf_resol = 50,
  pacific_centered = TRUE,
  as_gg = FALSE,
  lgl = NULL,
  ltl = NULL,
  lgb = NULL,
  ltb = NULL,
  ...
)
}
\arguments{
\item{sfmap}{World map data in sf format, Default: NULL}

\item{sf_resol}{Resolution of sf world map data passed to
\code{rnaturalearth::ne_countries(scale)}, Default: 50 (medium).}

\item{pacific_centered}{If TRUE (default), the world map is centred on the
Pacific Ocean; if FALSE, the world map is centred on the prime meridian (i.e.
the meridian on Greenwich).}

\item{as_gg}{Whether or not to make a gg object, Default: FALSE (not make)}

\item{lgl}{Longitude limits, Default: NULL}

\item{ltl}{Latitude limits, Default: NULL}

\item{lgb}{Longitude breaks, Default: NULL}

\item{ltb}{Latitude breaks, Default: NULL}

\item{...}{Arguments passed to \code{geom_sf}}
}
\value{
list or gg
}
\description{
This function is intended to be used in two ways: first, to
overlay a world map on an existing gg object, and second, to create a base
world map using ggplot2.
}
\details{
If sfmap is NULL, the default map projection system is a geographic
coordinate system based on the WGS84. It has not yet been checked whether
there are any problems with other coordinate reference systems.
}
\examples{
if(interactive()){
 library(ggplot2)
 library(frabento)
 df <- data.frame(Long = seq(140, 240, by = 5),
                  Lati = runif(n = 21, min = 40, max = 65),
                  group = c(rep(1:5, each = 4), 6))
 
 # Default: overlay worldmap on data points
 ggplot() +
   geom_point(data = df, aes(x = Long, y = Lati, color = as.factor(group))) +
   wmap_sf(lgl = c(135, 245), ltl = c(35, 70))
 
 # overlay data points on worldmap
 wmap_sf(as_gg = TRUE, lgl = c(135, 245), ltl = c(35, 70)) +
   geom_point(data = df, aes(x = Long, y = Lati, color = as.factor(group)))

 # plain world map as pacific centered (default)
 wmap_sf(as_gg = TRUE, pacific_centered = TRUE)
 }
}
\seealso{
\code{\link[rnaturalearth]{ne_countries}}
\code{\link[sf]{st_break_antimeridian}}, \code{\link[sf]{st_shift_longitude}}
}
